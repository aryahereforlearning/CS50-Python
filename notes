Today I learned that exceptions help my program handle errors instead of crashing. I understood that try is where I put the code that might fail, while except catches the error if something goes wrong. else runs only if nothing failed, which keeps successful code separate and easier to read. finally is for cleanup because it always runs, whether an error happens or not. The biggest takeaway for me is that exceptions make programs more reliable and user-friendly by expecting that things can go wrong.
